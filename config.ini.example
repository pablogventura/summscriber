# Copy to config.ini and add your API key.
# config.ini is in .gitignore and is not committed.
#
# Defaults (if omitted): base_url = https://api.openai.com/v1, model = gpt-4o-mini
#
# For Open WebUI (e.g. https://chat.ccad.unc.edu.ar): use base_url = .../api
# (the web UI uses /api/chat/completions; /ollama/v1 is the Ollama proxy, not OpenAI-style).

[openai]
api_key = sk-your-token-here
base_url = https://chat.ccad.unc.edu.ar/api
model = gpt-oss:20b
